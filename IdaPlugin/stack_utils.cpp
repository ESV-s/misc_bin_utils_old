#include "stack_utils.h"


/// \brief \n Заглушка: «живые» стеки пока не собираем. \n
/// \details Когда добавим реализацию, здесь появится код опроса отладчика/карт памяти.
bool GetProcessStackRegions(std::vector<StackRegion> &out)
{
	// if: вызывают сейчас — мы явно показываем, что функциональность пока не реализована
	out.clear();          // \brief \n на всякий случай очищаем целевой вектор \n
	return false;         // else: сообщаем вызывающему коду «ещё не готово»
}
